# Sudoku-Solver
This program is a Sudoku puzzle solver written in Python. It reads a 9x9 Sudoku grid from a text file, uses a backtracking search algorithm to fill in all empty cells, and prints the solved board. 
